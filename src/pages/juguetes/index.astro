---

import { getCollection } from 'astro:content';
const marcasEntries = await getCollection('marcas');

import JuguetesLayout from '../../layouts/JuguetesLayout.astro';
import CardMarca from '../../components/CardMarca.astro';

---


<JuguetesLayout>
    
    <div class="flex flex-1 overflow-y-auto mb-2 ">
        {/* Sección principal */}
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 px-4 gap-4">
        {
            marcasEntries.map((marca) => (
                <CardMarca marca={marca} />
            ))

        }
        </div>


    </div>

    
        <!-- <section >
			<div class="mx-auto flex px-8 py-2 lg:py-0 lg:flex-row flex-col items-center ">
                <div class="w-full lg:w-1/2 flex flex-col items-start text-left mb-8 md:mb-0 lg:order-first">
                    <Image
                        src={creativas_8}
                        alt="Imagen"
                        width={1110}
                        height={340}
                        class="object-cover object-center rounded-2xl "
                    />
                </div>
				<div class=" lg:w-1/2 w-full pl-0 lg:pl-20 z-10">
					<h1 class="text-xl mb-4 font-medium italic text-justify">Un universo de historias para mentes curiosas</h1>
					<blockquote class="text-base italic font-semibold text-gray-900">
						<svg class="w-8 h-8 text-gray-400 dark:text-gray-600 mb-4" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="currentColor" viewBox="0 0 18 14">
							<path d="M6 0H2a2 2 0 0 0-2 2v4a2 2 0 0 0 2 2h4v1a3 3 0 0 1-3 3H2a1 1 0 0 0 0 2h1a5.006 5.006 0 0 0 5-5V2a2 2 0 0 0-2-2Zm10 0h-4a2 2 0 0 0-2 2v4a2 2 0 0 0 2 2h4v1a3 3 0 0 1-3 3h-1a1 1 0 0 0 0 2h1a5.006 5.006 0 0 0 5-5V2a2 2 0 0 0-2-2Z"/>
						</svg>
                     <p class="mb-4 md:mb-8 text-justify">“Nuestros juguetes de madera eco-friendly están diseñados para estimular el desarrollo y la creatividad de los niños mientras respetan el medio ambiente. Fabricados con materiales sostenibles, cada juguete es una invitación a la exploración, el juego imaginativo y el aprendizaje a través del tacto. En nuestra juguetería, creemos que la diversión y el cuidado del planeta pueden ir de la mano.”</p> 
                    </blockquote>

				</div>
			</div>
		</section> -->
        <!-- <section class="bg-white dark:bg-gray-900">
            <div class="container px-6 py-10 mx-auto">
              <h1
                class="text-3xl font-semibold text-gray-800 capitalize lg:text-4xl dark:text-white"
              >
                {siteConfig.title}
              </h1>
        
              <div class="grid grid-cols-1 gap-8 mt-8 md:mt-16 md:grid-cols-2">
                {blogPosts.map((post) => <TypedBlogPost post={post} />)}
              </div>
            </div>
          </section> -->

</JuguetesLayout>

